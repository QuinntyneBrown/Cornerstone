# @cornerstone/ui

FaithTech's standalone, signal-based Angular component library. Import public APIs from
`@cornerstone/ui` and add `@cornerstone/ui/styles/theme.scss` to the consuming application.

The optional `@cornerstone/ui/styles/compat.scss` stylesheet supports incremental migration from
the audited Liturgy and Word Up markup without adding compatibility code to the default bundle.
